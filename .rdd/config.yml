# RDD Configuration
# Generated by RDD Framework

version: "1.0.0"
project:
  name: "aigw"
  description: "AI Gateway — litellm proxy Rust 最小兼容替代，支持云服务和企业自托管部署"

# Document language configuration
# Supported: en (English), zh (Chinese)
language: "zh"

# Stage settings
stage:
  min_coverage: 20
  max_failures: 3
  tech_debt_threshold: 3

# Gate settings
gates:
  design_required: true
  review_required: true
  e2e_required: true
  docs_required: true

# Hook settings
hooks:
  enabled: false
  config_file: ".rdd/config/hooks.yml"
